#ifndef TRUMP_H
#define TRUMP_H

/* show_trump_window() — Easter egg triggered by typing #trump into the document.
 * Displays a scrollable window that cannot be dismissed until the user has
 * scrolled all the way to the bottom, at which point any key will close it. */
void show_trump_window(void);

#endif /* TRUMP_H */
